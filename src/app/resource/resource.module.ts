// Imports from @angular
import { NgModule } from '@angular/core';
// Services
import { ResourceService } from './resource.service';

@NgModule({ providers: [ResourceService] })
export class ResourceModule { }
